startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        description: "Purple Flea Casino API key (get one at api.purpleflea.com)"
    required: []
  commandFunction:
    - node
    - dist/mcp/server.js
